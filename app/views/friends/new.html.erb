<!-- Validation failure messages -->
<% if @friend.errors.any? %>
  <% @friend.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Add Friend</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_friend" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for uid -->
      <div class="form-group">
        <label for="uid" class="control-label">
          Uid
        </label>

        <input type="text" id="uid" name="uid" placeholder="Uid" class="form-control" value="<%= @friend.uid %>">
      </div>

      <!-- Label and input for name -->
      <div class="form-group">
        <label for="name" class="control-label">
          Name
        </label>

        <input type="text" id="name" name="name" placeholder="Name" class="form-control" value="<%= @friend.name %>">
      </div>

      <button class="btn btn-success">
        Create Friend
      </button>
      or
      <a href="/friends">Cancel</a>
    </form>
  </div>
</div>
